Working on maps

- Demo of small models in the browser generating existing terrain from compressed internal representation.  Models are small enough that web workers each load it and do inference jobs on subsets.  Workers and job queue manage frustum and foveation. 
https://rowenwitt.github.io/neural_terrain/NEURAL_TERRAIN_EXAMPLE
